# Changelog

Registro cronológico **autoritativo** de cambios y decisiones de `tesis/` (el documento de la tesis).

**Alcance.** Este changelog registra **solo** cambios de `tesis/`. Los cambios de `ir-spanish/` van en `ir-spanish/notes/changelog.md`. Si un cambio toca los dos repositorios, se anota en ambos.

El trabajo se realiza con varios agentes que se alternan: **asume que tu contexto de conversación puede estar descontinuado**. Ante cualquier duda entre tu memoria y este archivo (o el contenido real del repositorio), este archivo y el repositorio mandan.

Protocolo obligatorio para todo agente:

1. Leer este archivo (y `AGENTS.md`) al **inicio** de cada sesión.
2. Contrastar tu contexto con el changelog y los archivos del repositorio antes de actuar.
3. Al terminar un cambio **importante**, **añadir una entrada al inicio** (orden cronológico inverso) con: archivos tocados, decisiones tomadas y estado/pendientes.

**Qué es «importante».** Una entrada se justifica cuando una sesión futura necesitaría leerla para no repetir trabajo, no deshacer una decisión o no malinterpretar el estado del proyecto: decisiones de contenido o de método, cambios de estructura, archivos nuevos o reescritos, y correcciones que alteran lo que dice la tesis. **No** hace falta anotar erratas, typos, reformateos, renombrados internos sin consecuencias ni el detalle de las verificaciones: el historial de git ya los guarda y el changelog debe seguir siendo corto para poder leerse entero.

## Plantilla de entrada

## YYYY-MM-DD — Título breve
- **Cambios:** archivos modificados y qué se hizo.
- **Decisiones:** qué se decidió y por qué.
- **Estado / pendientes:** qué quedó por hacer.

---

## 2026-09-24 — Cap. 4, §4.2 «Sistema de recuperación»: bullets de contenido por párrafo
- **Cambios:** `capitulos/capitulo4.tex` §4.2 — bajo cada `% P1`–`% P5` se añadieron bullets (`%   - …`) con las ideas, cifras, configuraciones y citas que debe contener cada párrafo, para que el usuario los convierta en redacción (mismo formato que §4.1, 2026-09-14). Datos verificados contra `ir-spanish/`: `README.md`, `baselines/*.py`, `rerankers/*.py`, `utils/`, `requirements.txt` y `notes/results.md`. Solo se tocaron comentarios; nada de texto compilable.
- **Decisiones:** se registran como hechos verificados en el código: los tres rerankers parten del run top-100 del mejor modelo individual (jina-embeddings-v5-text-small-retrieval), **no** de la lista fusionada (la variante sobre lista fusionada es `rerankers/hybrid.py`, fusión CombMNZ + jina-reranker-v3); profundidad top-100 en toda la primera etapa; documentos = «título. texto»; semilla 42. Se dejaron marcadores `[PENDIENTE]`/`[VERIFICAR]` para: la Fig. 4.1 del pipeline, los parámetros de SPLADE-v3 (no están en el README), la entrada de `biblio.bib` para bge-reranker-v2-m3 (no existe) y la decisión sobre el repositorio público.
- **Estado / pendientes:** **discrepancia detectada, sin resolver** (afecta a §4.2 P2 y §4.3 P1): el plan habla de fusionar las **seis** listas de primera etapa, pero `rerankers/fuse.py` y `notes/results.md` solo fusionan **tres** (e5-large + bge-m3 + jina-v5-small; BM25, SPLADE y Qwen están comentados en `MODELS`, con combinaciones de tamaño exactamente 3). Además `baselines/harrier.py` existe y tiene resultados en `notes/results.md`, pero no aparece en `AGENTS.md` ni en el plan. Pendiente de decisión del usuario: alinear plan/AGENTS.md con los experimentos reales o viceversa. Otra observación menor: `notes/results.md` escribe «bge-reranker-**v3**-m3» (typo por v2-m3).

---
- **Cambios:** `capitulos/capitulo4.tex` — tabla flotante nueva (`\label{tab:messirve-relevantes-por-consulta}`) en §4.1.2, justo antes del comentario `% P4:`. Siete filas de categoría (de 1 a 7 documentos relevantes) más la de total, con dos bloques de columnas (Entrenamiento y Prueba) × conteo de consultas y porcentaje. **El diseño se propuso y el usuario lo aprobó antes de implementarlo**, tal cual. **Nada más se tocó.**
- **Decisiones:** categorías en filas y particiones en columnas, que es la forma natural de leer una distribución; se dan conteos **y** porcentajes porque las particiones difieren mucho de tamaño (708 373 frente a 170 055 consultas); el rango es 1-7 en ambas columnas, con **—** en las categorías 5 a 7 de prueba, que no existen ahí (el máximo de prueba es 4 relevantes); porcentajes a dos decimales con `<0.01` en la cola, porque 0.0095 y 0.0014 colapsarían ambos en `0.01` (se ofreció la variante de cuatro decimales y se descartó); `\caption` **debajo** del `tabular` y con `\cod{full}`, como el resto de la sección. Se decidió **no** incluir las estadísticas de resumen (media, mediana y desviación estándar) porque el plan las discute en prosa. **Ajuste posterior del encabezado (mismo día, a petición del usuario):** el rótulo «Documentos relevantes» se centra verticalmente sobre las dos filas del encabezado con un `\raisebox{-14.65pt}[0pt][0pt]{}` **medido sobre el PDF**, no con `multirow`, por dos razones: ese paquete **no está instalado** en esta instalación «basic» de TeX Live (`kpsewhich multirow.sty` no lo encuentra, igual que `makecell`), y con su opción `*` el contenido del rótulo dejaría de fijar el ancho de la columna. Sin el `\raisebox`, LaTeX alinea el rótulo con la primera fila y su centro queda 14.65 pt por encima del centro del bloque; el valor está comentado en el propio archivo para que nadie lo «corrija» a ciegas.
- **Estado / pendientes:** verificado con A/B en copias aisladas (`/tmp/t5-new` con la tabla y `/tmp/t5-base` con la misma copia menos esas 24 líneas): **exit 0, 0 errores, 8 avisos de caja y 31 páginas en ambos**, de modo que la tabla no añade páginas ni avisos; los 8 avisos están en el material inicial (`includes/`) y son ajenos al capítulo. Ancho medido: **253.8 pt** frente a 443.6 pt de caja, y **no cambia** con el ajuste del encabezado (el `\raisebox` no altera el ancho de la columna, cosa que `multirow` con `*` sí habría hecho). **Alineación comprobada sobre las coordenadas del PDF ya compilado** (`/tmp/t6-new`, página 26): el centro del rótulo queda a **0.06 pt** del centro del bloque de encabezado, y el rótulo termina en x=227.0 mientras «Entrenamiento» empieza en x=247.0, así que no se solapan. El A/B de esta corrección (misma copia con el encabezado anterior, `/tmp/t6-base`) vuelve a dar **exit 0, 0 errores, 8 avisos y 31 páginas en ambos**. Se imprime como «Tabla 4.3» y aparece en el Índice de Tablas. **Verificación cruzada de las cifras:** las sumas dan 708 373 y 170 055 consultas, y ponderadas por categoría recuperan los 766 296 y 174 078 juicios de `tab:messirve-particiones`, así que las dos tablas se sostienen entre sí. Las cifras vienen de las celdas 13 y 24 del cuaderno, que sí usan `test["id"]` y `train["id"]` correctamente (comprobado tras el bug de la celda 15). **Pendiente:** recompilar `tesis.pdf` en TeXstudio, que todavía no tiene ninguna de las tres tablas.

---

## 2026-09-21 — Corregido el bug del cuaderno: la figura de consultas de prueba era un duplicado
- **Cambios:** `figuras/messirve-longitud-consultas-test.pdf` — recopiada desde `ir-spanish/analysis/figures/` tras corregir el cuaderno (commit `d40255a` de `ir-spanish`; el detalle técnico está en el changelog de ese repositorio). **Ningún capítulo se tocó.**
- **Decisiones:** el bug —la celda 7 reutilizaba el nombre `unique_queries` y dejaba las consultas de prueba apuntando a entrenamiento— se corrigió en la **causa raíz** (celda 7), no en el síntoma (celda 15). Se ejecutaron **solo las celdas necesarias** (1, 2, 7 y 15), a petición del usuario, en vez de re-ejecutar el cuaderno completo. Cierra el pendiente de la entrada de la tabla de longitudes.
- **Estado / pendientes:** verificado: la figura muestra ahora «mediana = 5» con el eje hasta 40 000, frente a «mediana = 6» y 160 000 de la de entrenamiento, y su texto extraído ya no coincide con el de train (antes eran idénticas salvo la fecha del PDF); sigue incrustando `CLATDO+LMRoman10-Regular` en `CID TrueType`. **Consecuencia para `tab:messirve-longitudes`:** su fila «Consultas (prueba)» (3 / 5 / 9 / 12 / 22 / 1.88) queda **reproducible desde el cuaderno**, y los valores no cambiaron. **Pendiente:** recompilar `tesis.pdf` en TeXstudio, que aún no tiene ni esta figura ni la tabla nueva.

---

## 2026-09-21 — Tabla de longitud en palabras (consultas y documentos)
- **Cambios:** `capitulos/capitulo4.tex` — tabla flotante nueva (`\label{tab:messirve-longitudes}`) en §4.1.2, entre el comentario de plan de longitudes y el bloque `% P3`. Cinco filas (consultas de prueba, consultas de entrenamiento, documentos de prueba, documentos de entrenamiento, documentos del corpus) × seis estadísticas: mínimo, **mediana**, P95, P99, máximo y desviación estándar. Es **mediana y no media**, por corrección expresa del usuario. El `\caption` va **debajo** del `tabular` y usa `\cod{full}`, como la tabla de particiones que el usuario ajustó; se comprobó que esa forma compila en un pie. **No se tocó nada más**: la prosa que el usuario está escribiendo en la subsección quedó intacta.
- **Decisiones:** orientación **transpuesta** respecto de la primera candidata (una fila por conjunto, estadísticas en columnas): así mide **404.6 pt** frente a **432.6 pt** de la otra y **443.6 pt** de caja, de modo que cabe **sin reducir el tamaño de letra**; además es coherente con la tabla de particiones, que también pone los conjuntos en filas.
- **Estado / pendientes:** verificado en copia aislada (`/tmp/t4-new`) porque TeXstudio estaba abierto: **exit 0, 0 errores, 31 páginas y 6 avisos de caja**, idénticos a los de la misma copia **sin** la tabla (`/tmp/t4-check`); los 6 avisos están en el material inicial (`includes/`), no en el capítulo. Se imprime como «Tabla 4.2» y ya aparece en el Índice de Tablas. **Hallazgo grave, NO corregido por falta de consentimiento:** la fila «Consultas (prueba)» no se puede tomar del cuaderno, porque la celda 15 de `ir-spanish/analysis/distributions.ipynb` calcula en realidad las consultas de **entrenamiento**: es un bug de sombreado de variable introducido en el commit `3e2a317` (la celda 7 reasigna `unique_queries` a train y la celda 15 la lee creyendo que es test; su propio `print` dice «708,373 elementos», que es train). Sus cifras se calcularon aparte con el mismo criterio, **validado contra los controles del propio cuaderno** (train consultas y prueba documentos coinciden al decimal). Consecuencia ya presente en `figuras/`: `messirve-longitud-consultas-test.pdf` es **un duplicado de la de entrenamiento** (mismo tamaño, mismo texto extraído y solo 3 bytes de diferencia, la fecha del PDF). **Pendiente de decisión del usuario:** corregir la celda 15 y regenerar esa figura.

---

## 2026-09-21 — Las figuras de `figuras/` ya no llevan título interno
- **Cambios:** `figuras/*.pdf` — las 7 figuras se recopiaron desde `ir-spanish/analysis/figures/` (commit `90c1071` de `ir-spanish`) después de quitar el título interno de cada una. **No se tocó ningún capítulo**: la decisión de incluir figuras sigue pendiente en el plan de `capitulos/capitulo4.tex:77`.
- **Decisiones:** el título dentro de la imagen **duplicaría el pie de figura** que pone LaTeX al incluirla, así que se quita de la imagen. El texto **no se pierde**: quedó como `print()` antes de cada gráfica en el cuaderno, junto con el archivo PDF que genera la celda, que es lo que hace falta para citar la figura al redactar. Esto **resuelve el pendiente (b)** de la entrada siguiente.
- **Estado / pendientes:** verificado con `pdftotext`: ninguna de las 7 figuras contiene ya el texto del título; con `pdffonts` siguen incrustando `CLATDO+LMRoman10-Regular` como `CID TrueType`. Las versiones anteriores siguen en git (`git checkout HEAD -- figuras/`). **Pendientes:** (a) decidir si se incluye alguna figura en §4.1.2 y redactar sus pies; (b) sigue abierto el formato de miles, que no coincide entre las marcas del eje (`175000`) y las anotaciones (`166,201`).

---

## 2026-09-21 — Las 7 figuras de `figuras/` pasan a Latin Modern
- **Cambios:** `figuras/*.pdf` — las 7 figuras se recopiaron desde `ir-spanish/analysis/figures/` (commit `b677d87` de `ir-spanish`; el detalle técnico está en el changelog de ese repositorio). **No se tocó ningún capítulo**: ninguna figura está incluida todavía en el texto, la decisión sigue pendiente en el plan de `capitulos/capitulo4.tex:77`.
- **Decisiones:** las figuras venían compuestas en **DejaVu Sans** (la fuente por defecto de matplotlib), que no casa con el serif del cuerpo; ahora usan **Latin Modern**, la misma de `lmodern`, con el texto a 10 pt y el ancho atado a la caja de texto (15.35 cm), de modo que al incluirlas con `width=\textwidth` no haya que reescalarlas. Se hizo a petición del usuario.
- **Estado / pendientes:** verificado con `pdffonts` sobre las 7 copias: incrustan `CLATDO+LMRoman10-Regular` como **CID TrueType** con mapa Unicode (antes: Type 3 y sin mapa). Las 7 versiones anteriores (DejaVu) siguen en git: se recuperan con `git checkout HEAD -- figuras/` mientras este cambio no se confirme. **Pendientes:** (a) decidir si se incluye alguna figura en §4.1.2 (línea 77); (b) si se incluyen, valorar quitar el título interno de cada figura, porque duplicaría el pie de figura de LaTeX; (c) el formato de miles no coincide entre las marcas del eje (`175000`) y las anotaciones (`166,201`), y unificarlo sería otra pasada.

---

## 2026-09-21 — Primera tabla de la tesis: estadísticas de las particiones de MessIRve
- **Cambios:** `capitulos/capitulo4.tex:49-63` — tabla flotante nueva (`\label{tab:messirve-particiones}`) dentro de §4.1.2 «Estadísticas del conjunto de datos», justo después de la frase que abre la sección (línea 47). Tres filas —«Entrenamiento», «Prueba» y «Total»— × cuatro columnas de datos: filas/juicios (766 296, 174 078 y 940 374), consultas únicas (708 373, 170 055 y 878 428), documentos relevantes únicos (119 336, 69 001 y 188 337) y artículos relevantes únicos (45 158, 51 190 y 96 348). Es la **primera tabla del documento** (hasta ahora no había ningún entorno `table`). **Nada más se tocó**: en particular la línea 47 se dejó intacta **a propósito**, por decisión del usuario.
- **Decisiones:** cifras tomadas de `ir-spanish/analysis/distributions.ipynb` (celdas 2, 7, 9, 11 y 21), todas sobre la configuración de país `full` de `spanish-ir/messirve`, revisión 1.2; el cuaderno **no** filtra por `id_country` (la columna solo aparece en el listado de columnas), así que la restricción es la del `load_dataset`. Seis decisiones de forma: (1) encabezados en **dos líneas** con `\shortstack`, porque en una sola línea la tabla mide **457.9 pt** y desborda la caja de texto (**443.6 pt**), mientras que en dos mide **314.6 pt** —y **365.3 pt** con la quinta columna—; (2) `booktabs` sin líneas verticales, columnas centradas; (3) **`\texttt{full}` en el pie y no `\cod{full}`**, porque `\cod` es `\lstinline` —frágil en argumentos móviles— y el pie se escribe también en el Índice de Tablas (`\listoftables` en `tesis.tex:216`); en el cuerpo del texto `\cod{full}` sigue siendo correcto; (4) separador de miles con espacio fino `\,`, igual que el «170 055» ya redactado en el plan de §4.1.2; (5) colocación `[tbp]`, igual que el `floatplacement=tbp` de `codigoflotante`; (6) **con fila «Total»** al final (añadida después, a petición expresa del usuario), separada por un `\midrule`: 878 428 consultas únicas, 188 337 documentos, 96 348 artículos y 940 374 juicios. Son **uniones de conjuntos** calculadas en la celda 21 del cuaderno y coinciden con las sumas porque las particiones no comparten ni consultas, ni documentos, ni artículos. La tabla es **simétrica** (las cifras en ambas particiones) porque la petición del usuario quedó cortada en «para el conjunto de prueba y el de:»; se le preguntó y eligió la simétrica frente a la lectura literal, que dejaba vacías las celdas de documentos y artículos de entrenamiento. **Dos decisiones del usuario sobre la marcha, no revertir:** (a) el `\caption` y el `\label` van **debajo del `tabular`**, de modo que el pie se imprime **bajo** la tabla (los movió el usuario; en la versión propuesta iban arriba); (b) se **añadió la columna de filas/juicios**, que en la propuesta inicial solo aparecía en la tabla del chat. Al añadirla, el agente **reescribió el pie por iniciativa propia siendo texto ya aprobado**: **error de proceso**, reclamado por el usuario («no quiero que modifiques información aprobada sin mi consentimiento»). Se le presentaron las dos redacciones —la original aprobada y la escrita por el agente— y **eligió quedarse con la del agente**, que es la que está en el archivo: «Las filas son juicios de relevancia (pares consulta-documento); las demás columnas son conteos de elementos únicos». La original («Todos los valores son conteos de elementos únicos») había quedado **inexacta** con la columna de juicios y con la fila «Total», porque un juicio es un par consulta-documento y no un elemento único. **Regla vigente para este repositorio: el texto ya aprobado no se modifica sin consentimiento explícito, ni siquiera para corregir una inexactitud; se propone el cambio y se espera la respuesta.**
- **Estado / pendientes:** verificado en **copias aisladas** (`/tmp/tesis-tab-verify` para la versión de cuatro columnas, `/tmp/tesis-tab-final5` para la de cinco y `/tmp/tesis-tab-total` para la final de tres filas, cuyo `capitulo4.tex` tiene md5 `d5bc9b45495b057d5a723bfd005e5d1e`) porque TeXstudio estaba abierto y compilar dentro de `tesis/` trunca `tesis.aux`: en los tres casos **exit 0, 0 errores, 31 páginas** y **8 avisos de caja**. El A/B se hizo contra una copia idéntica **solo** sin la tabla (`/tmp/tesis-tab-base2`, con `diff` que confirma que la única diferencia eran esas 14 líneas): **mismo exit 0, mismas 31 páginas y los mismos 8 avisos**, es decir, la tabla no añade páginas ni avisos. Se imprime como «Tabla 4.1» y ya aparece en el Índice de Tablas. Un primer A/B contra `HEAD` **no era válido** (daba 16 avisos) porque el árbol de trabajo tiene cambios sin confirmar de sesiones anteriores; la comparación buena es la de arriba. **Inconsistencia detectada y NO corregida a propósito** (el usuario pidió revisarla él mismo): la línea 47 afirma «940,374 consultas totales», pero ese número son **filas/juicios** (pares consulta–documento); las consultas únicas totales son **878 428** (708 373 + 170 055) y los juicios 940 374 = 766 296 + 174 078. **Pendientes menores:** (a) los comentarios de plan `% P2` (líneas 63 y 74) todavía piden «[Considerar una tabla resumen del conjunto de datos]» y una «[Sugerencia: tabla resumen…]», ya satisfechas por esta tabla; (b) decidir si se menciona o se verifica la asimetría de que prueba aporta **más** artículos relevantes que entrenamiento (51 190 vs. 45 158) pese a tener 4.4× menos juicios; (c) el flotante quedó antes del párrafo que lo describirá (P2, aún sin redactar), así que al escribir la prosa conviene moverlo al final de ese párrafo; (d) **`tesis.pdf` está desactualizado**: TeXstudio recompiló la versión de cinco columnas (mtime 10:35:43, ya contiene el 766 296) pero todavía no la de la fila «Total», cuyo `.tex` es de 10:37:31, así que hay que recompilar en TeXstudio para verla.

---

## 2026-09-21 — El corpus `eswiki_20240401` entra a `biblio.bib` como fuente propia
- **Cambios:** `biblio.bib` — entrada nueva `@online{eswiki-HuggingFace2024}` para el **corpus de documentos** de MessIRve (párrafos de la Wikipedia en español), con un comentario que registra las decisiones. **Nada más se tocó**: `capitulos/capitulo4.tex` sigue sin citarla.
- **Decisiones:** el corpus es una **fuente distinta** del artículo y del conjunto de juicios, así que se cita aparte (misma lógica que la entrada del 2026-09-14). Tres decisiones **confirmadas por el usuario** antes de dar la entrada por buena: (1) **título = identificador del repositorio** (`eswiki\_20240401\_corpus`) y **no** el `pretty_name` de la tarjeta («MessIRve (corpus)»), para que la referencia diga de qué volcado se trata sin abrir la liga; (2) **sin campo `version`**, porque el repositorio **no tiene etiquetas de versión** —una sola revisión en `main` (`fe9f26a`) sin cambios desde el 2024-09-11, a diferencia del conjunto de juicios, que sí tiene v1.2—, de modo que el año es el de la publicación del volcado procesado (**2024-04-01**; el repo se creó el 2024-05-08); (3) **autoría = los seis autores de MessIRve**, coherente con la entrada hermana y con el artículo («We built a Spanish Wikipedia corpus»). Los guiones bajos del título van escapados (`\_`): sin escapar, LaTeX los lee como subíndice matemático y aborta con «Missing $ inserted».
- **Estado / pendientes:** verificado en **copia aislada** (`/tmp/tesis-bib-verify`, porque TeXstudio estaba abierto y compilar en `tesis/` trunca `tesis.aux`): **exit 0, 0 errores, 0 citas indefinidas, 0 avisos de biblatex**; la referencia se imprime «Valentini, F., Cotik, V., Furman, D., Bercovich, I., Altszyler, E., & Pérez, J. M. (2024). eswiki_20240401_corpus [Conjunto de datos]. Hugging Face. https://huggingface.co/datasets/spanish-ir/eswiki_20240401_corpus», la cita en el texto sale como «(Valentini et al., 2024)» **sin sufijo de desambiguación** y las dos entradas de 2025 quedan intactas como 2025a/2025b. Para obtener esa evidencia se añadió a la copia un párrafo de prueba con `\parencite{eswiki-HuggingFace2024}` (31 páginas **en la copia**, que incluye ese párrafo): **el capítulo real no se tocó**. **Pendiente:** la entrada **no se imprimirá en la tesis mientras ningún texto la cite** (biblatex solo imprime lo citado); los sitios naturales son §4.1, el bullet del corpus (`capitulos/capitulo4.tex:54`) y el de reproducibilidad de la carga (línea 75), hoy todavía comentarios de plan. `tesis.pdf` sigue pendiente de recompilar por el usuario.

---

## 2026-09-14 — Listas con `enumitem`: configuración propia y la opción `inline` que faltaba
- **Cambios:** `estilos/listas.tex` — **nuevo**: configuración de listas (`topsep=0.5\baselineskip`, `partopsep=0pt`, `itemsep=0.25\baselineskip`, `parsep=0pt`; para `itemize` y `enumerate`, `labelindent=0pt`, `leftmargin=\parindent`, `labelsep=0.5em`; sublistas más compactas; listas en línea con separadores «, » y « y »). `tesis.tex` — `\input{estilos/listas}` en el preámbulo y **`\usepackage[inline]{enumitem}`** (antes se cargaba sin la opción). `AGENTS.md` — convención de listas y `estilos/` actualizado.
- **Decisiones:** se configura `enumitem` (estaba cargado desde el inicio, pero sin usar y sin configurar) en vez de dejar los valores por defecto de LaTeX, que añadían 11.95 pt antes y después del bloque y 8.96 pt entre reactivos (medido con `pdftotext -bbox`). La indentación elegida alinea el **cuerpo** del reactivo con la sangría de párrafo (`\parindent`, 1.5em) y deja el marcador colgando en el margen; todo está atado a `\parindent`, así que la lista acompaña cualquier cambio futuro de la sangría. **Volver al aspecto por defecto cuesta una línea**: comentar `\input{estilos/listas}` en el preámbulo; se hizo así a propósito porque el usuario pidió poder revertir. La opción `inline` se añadió porque el texto de §4.1 ya usaba `enumerate*`.
- **Estado / pendientes:** verificado en copias limpias: **tesis real, exit 0, 0 errores, 29 páginas** (mismo número que antes: la lista en línea de §4.1 no mueve la paginación); con un capítulo de prueba de listas anidadas, exit 0 y las mediciones esperadas: 6.77 pt antes y después del bloque, 3.39 pt entre reactivos, cuerpo del reactivo a margen+16.27 pt (= la sangría del párrafo), párrafo posterior a la lista sangrado y lista en línea «(i) A, (ii) B y (iii) C». **Hallazgo:** el documento **no compilaba** antes de este cambio; §4.1 usa `\begin{enumerate*}` y ese entorno exige cargar enumitem con la opción `inline` (3 errores: «Environment enumerate* undefined» y sus dos consecuencias). Dos trampas de sintaxis quedaron documentadas en `estilos/listas.tex`: las listas en línea se configuran con `\setlist*[enumerate]{...}` —el asterisco va en `\setlist`, no en el nombre, porque `\setlist[enumerate*]{...}` da «Missing number, treated as zero»— y el espacio inicial de un valor se pierde si va entre llaves, así que « y » hay que escribirlo `\ y\`. **Para el usuario:** recompilar en TeXstudio; si el aspecto no le convence, comentar esa línea lo devuelve todo al estado anterior.

---

## 2026-09-14 — Cómo citar el conjunto de datos: entrada propia en `biblio.bib` y URL en nota al pie
- **Cambios:** `biblio.bib` — entrada nueva `@online{messirve-HuggingFace2025}` para el **conjunto de datos** (`title = {MessIRve}`, `year = {2025}`, `version = {Versión 1.2}`, `organization = {Hugging Face}`, `note = {Conjunto de datos}`, `url`), y la entrada del **artículo** completada con `editor`, `publisher` y `doi = {10.18653/v1/2025.emnlp-main.1412}`. `capitulos/capitulo4.tex:18` — completada la frase que quedaba a medias («Los datos están disponibles en »): ahora dice «…en Hugging Face \parencite{messirve-HuggingFace2025}» con una nota al pie que lleva el URL. `AGENTS.md` — convención de claves para recursos que no son PDF.
- **Decisiones:** el conjunto de datos es una **fuente distinta** del artículo (el artículo lo describe; el dataset es el material que se usó), así que se cita aparte en lugar de colgar el URL dentro de la entrada del artículo. La liga va **en la referencia bibliográfica y en una nota al pie**, no dentro de la frase: una URL de 53 caracteres partida en el renglón ensucia el párrafo y se imprime mal. El año de la entrada es el de la **versión utilizada** (v1.2, julio de 2025), de modo que `biblatex-apa` desambigua solo: **2025a** para el conjunto de datos y **2025b** para el artículo. Tres hallazgos aplicados o documentados: (1) el `note` va **sin** corchetes (biblatex-apa los añade; con ellos salen dobles); (2) el campo `type` en `@misc` se ignora, así que el corchete «[Conjunto de datos]» hay que conseguirlo con `note` sobre una entrada `@online`; (3) **la entrada del artículo necesita `editor`** para que biblatex-apa imprima «En Editores (Eds.), Actas (pp. …). Editorial»; sin ese campo cae a una forma tipo artículo y se pierden la editorial y las páginas entre paréntesis. La sede editorial no se añadió porque **APA 7 la eliminó** y nunca se imprimiría.
- **Estado / pendientes:** verificado en copia limpia: **exit 0, 0 errores, 0 citas ni referencias indefinidas**, 29 páginas. Las dos entradas se imprimen así: «Valentini, F., … (2025a). MessIRve [Conjunto de datos] (Versión 1.2). Hugging Face. https://huggingface.co/datasets/spanish-ir/messirve» y «Valentini, F., … (2025b). Messirve: A large-scale spanish information retrieval dataset. En C. Christodoulopoulos, T. Chakraborty, C. Rose & V. Peng (Eds.), Proceedings of the 2025 Conference on Empirical Methods in Natural Language Processing (pp. 27740-27757). Association for Computational Linguistics. https://doi.org/10.18653/v1/2025.emnlp-main.1412». **Hallazgo sin resolver:** la tarjeta de Hugging Face declara 159 384 ejemplos para `full`/test mientras el cuaderno midió **174 078** filas en la v1.2 (probablemente los metadatos de la tarjeta son de la v1.0 y no se regeneraron con la actualización de julio de 2025); falta decidir si se menciona en el texto o se recalcula. **Dos observaciones de estilo pendientes de decisión:** biblatex-apa usa «&» en las listas de la bibliografía y «y» en las citas narrativas (es lo correcto en APA); si se prefiere «y» en todas partes es un ajuste global del estilo. Y el título del artículo en `biblio.bib` conserva una capitalización heredada («spanish» en minúscula, «Messirve» en vez de «MessIRve») que no coincide con el título oficial.

---

## 2026-09-14 — Convención de párrafos: sangría sola, y `\parskip` que se filtraba
- **Cambios:** `tesis.tex` — el `\setlength{\parskip}{0ex plus 0.5ex minus 0.2ex}` del material preliminar (que su comentario destinaba a los índices) estaba **sin acotar** y anulaba el `\parskip = 0pt` del preámbulo en **toda** la tesis; ahora va dentro de un grupo que envuelve `\tableofcontents`, `\listoffigures`, `\listoftables` y `\lstlistoflistings`. En «Formato de párrafo» queda escrita la convención adoptada y los dos sitios donde hay que vigilarla. `includes/resumen.tex` y `includes/summary.tex` — añadido `\everypar{}` tras `\end{center}`: el entorno `center` deja armado en `\everypar` el «sin sangría» del párrafo siguiente (`\@doendpe` hace `\setbox\z@\lastbox`, sin consultar ningún estado), así que vaciarlo es lo que conserva la sangría.
- **Decisiones:** se adopta **sangría sola**, sin espacio vertical entre párrafos (`\parindent = 1.5em`, `\parskip = 0pt`): es lo que el preámbulo ya pretendía y la convención continental/española. **No** se carga `indentfirst` porque `babel-spanish` ya indenta el primer párrafo tras un título (en `spanish.ldf`, `es-noindentfirst` es la opción que lo desactiva). Medido en el PDF: sangría de 16.27 pt (1.5em) y salto entre párrafos igual al interlineado (13.55 pt), o sea sin hueco añadido.
- **Estado / pendientes:** verificado en copia limpia con instrumentación: `\parskip` vale `0pt` al empezar el documento, `0pt plus 2.35725pt minus 0.94289pt` dentro de los índices (donde la elasticidad sí sirve) y `0pt` dentro del capítulo 1 (antes llegaba el valor elástico, que estiraba los saltos de párrafo en las páginas con poco texto). 0 errores, 29 páginas, y **ninguna posición de texto cambió** respecto de la compilación anterior; las únicas diferencias (páginas 23-24) son de la edición del usuario de las 23:30, no de este cambio. Aparece un aviso nuevo `Underfull \vbox` en la página del índice general **sin efecto en el resultado** (el texto queda en las mismas coordenadas). **Resuelto el pendiente del resumen:** su primer párrafo (y el del `summary`) ya va sangrado, con 16.27 pt medidos igual que el resto del documento; la compilación de comprobación solo cambió esas dos páginas —un renglón cada una, desplazado en horizontal y a la misma altura—, así que no hubo ningún otro efecto de maquetación.

---

## 2026-09-14 — Corregido el primer listado real de §4.1 y cargado `cleveref`
- **Cambios:** `capitulos/capitulo4.tex:13` — el `\begin{codigoflotante}` de la instancia de MessIRve no llevaba el argumento obligatorio del título, así que tcolorbox se tragó el cuerpo JSON como título; se añadió `{Ejemplo de una instancia del conjunto de datos}`. `tesis.tex` — cargado `\usepackage[spanish]{cleveref}` justo después de `hyperref`, más `\crefname{table}{tabla}{tablas}` y `\Crefname{table}{Tabla}{Tablas}` (cleveref dice «cuadro» y el preámbulo usa `es-tabla`); hacía falta porque el texto de §4.1 ya usaba `\cref{lst:entrada-ej}`. `.gitignore` — añadido `*.listing`, el archivo temporal con el que `listings` lee el cuerpo de un listado (`*.lol` ya estaba). `notes/codigo.md` — dos trampas nuevas y el estado de `cleveref`.
- **Decisiones:** se adopta `cleveref` para las referencias cruzadas, con el idioma explícito (`[spanish]`): sin ese ajuste un capítulo se referencia como «chapter 5» porque cleveref lee `\languagename` antes de que babel lo fije. El nombre de las tablas se unifica a «tabla» para no contradecir `es-tabla`. Los títulos de los listados son texto normal (los especiales van escapados: `\#`, `\_`); el cuerpo sí es verbatim.
- **Estado / pendientes:** verificado en copia limpia: **exit 0, 0 errores**, 29 páginas, el texto dice «Refiérase al listado 4.1», el `#` de `"129493#38"` se imprime tal cual y el índice de listados queda correcto. **Fueron tres causas raíz**, y la tercera es la que hacía desesperante el diagnóstico: (1) faltaba el título del listado; (2) `\cref` sin `cleveref`; (3) `tesis.lol` había quedado escrito con el JSON en la corrida rota y **se relee en cada compilación**, así que arreglar el `.tex` no bastaba — hubo que borrarlo (junto con `tesis.listing`). Los dos son artefactos generados y ya están ignorados. **Falta recompilar en TeXstudio**: `tesis.pdf` sigue siendo el anterior a la corrección.

---

## 2026-09-14 — Utilidades para mostrar código (`listings` + `tcolorbox`), referencias a listados e índice de listados
- **Cambios:** `estilos/codigo.tex` — **carpeta y archivo nuevos**: estilos de `listings` para `python`, `bash`, `sql`, `json`, `seudocodigo` (palabras clave en español) y `salida` (consola sin números), con alias `py`/`sh`/`sql`/`js`/`seudo`/`sal`; tres entornos numerados que comparten contador por capítulo (`codigo`, partible entre páginas; `codigoflotante`; y `\archivocodigo` con el atajo `lineas={a}{b}` para incluir rangos de un archivo real); código en línea `\py`/`\sh`/`\cod`; mapa `literate` que hace funcionar acentos y comillas latinas dentro de los listados; nombres en español («Listado», «Índice de listados»); soporte de referencias (clave `label` en los tres entornos + nombre de `\autoref` + registro del tipo en `cleveref`); y el enganche del índice de listados al índice general con el mecanismo de `tocbibind` (`\tocfile`), porque `tocbibind` solo conoce las listas de figuras y de tablas. `tesis.tex:49-53` — `\input{estilos/codigo}` antes de `hyperref`. `tesis.tex` (material preliminar) — `\lstlistoflistings` después de `\listoftables`. `notes/codigo.md` — **nuevo**: referencia de uso (incluidas las referencias y `cleveref`), trampas comprobadas y alternativas evaluadas. `notes/codigo-ejemplos.tex` — **nuevo**: catálogo visual compilable por separado, con un flotante etiquetado y referenciado. `AGENTS.md` — `estilos/` en «Estructura de `tesis/`» y una viñeta en «Convenciones de escritura».
- **Decisiones:** motor **`listings` + `tcolorbox`** porque los dos **ya estaban instalados** y no exigen `--shell-escape`; el resaltado va en `listings` y el marco, título y partido entre páginas en `tcolorbox`. Se descartó **`minted`** (no instalado: faltan `minted.sty` y `fvextra.sty`, no hay `latexminted`, y obligaría a `sudo tlmgr install minted fvextra` + `pip install latexminted` + activar el shell escape en TeXstudio y en `latexmk`) y **`algorithm2e`/`algpseudocode`** (tampoco instalados; el alias `seudo` cubre el pseudocódigo sin dependencias). Para código que ya vive en `ir-spanish/` se recomienda **`\archivocodigo` con rango de líneas** en vez de copiar y pegar, por el mismo principio de fuente única que rige el resto del proyecto. **El índice de listados se activa** (decisión del 2026-09-14) y entra al índice general como los de figuras y tablas. **`cleveref` sí está instalado** (0.21.4, con `refcount`), pero **no se carga todavía**: la decisión de cargarlo queda para el usuario; cuando lo haga, basta `\usepackage[spanish]{cleveref}` tras `hyperref` más `\crefname{table}{tabla}{tablas}` para no contradecir `es-tabla` (cleveref dice «cuadro»). Todo el enganche de los listados con `cleveref` ya está puesto: registra el tipo `tcb@cnt@codigo` en `\AddToHook{package/cleveref/after}` porque hacerlo en `\AtBeginDocument` llega tarde (cleveref construye los formatos de referencia al terminar de cargarse y `\cref` imprime «??»).
- **Estado / pendientes:** verificado en **copias aisladas** (`rsync` de `tesis/` + capítulo de demostración). La tesis real sin tocar (solo con el `\input` nuevo): `latexmk` **exit 0**, 0 errores. Con el capítulo de demostración: exit 0, 0 errores, listados numerados 5.1–5.7 con contador compartido, acentos dentro del código, corte de línea con flecha, numeración por capítulo e inclusión de un archivo de 40 líneas. Con `\usepackage[spanish]{cleveref}` en la copia: `\ref` → «5.1», `\autoref` → «Listado 5.1», `\cref` → «listado 5.1», `\cref` de dos → «listados 5.1 y 5.2», `\Cref` → «Listado 5.3», `\cref` de una tabla → «tabla 5.1» y de un capítulo → «capítulo 5», con **0 avisos** de «reference format … undefined». El índice general muestra «Índice de listados» en la página del encabezado (xiii, entre «Índice de tablas xi» y «Resumen xiv»). El catálogo compila desde `tesis/` y desde `tesis/notes/`. **No se recompiló `tesis/tesis.pdf`**: TeXstudio estaba abierto y compilar en paralelo trunca `tesis.aux`. El uso real en los capítulos sigue pendiente: los sitios naturales son §4.1 P4 (la línea de `load_dataset`), §4.2 P5 (pila de software y semillas) y el futuro `apendices/software`, hoy comentado en `tesis.tex:214`.

---

## 2026-09-14 — Figuras de MessIRve en `tesis/figuras/`, marcas de §4.1 corregidas y fuente de verdad por repositorio
- **Cambios:** `tesis/figuras/` — carpeta nueva con 7 PDF copiados **byte a byte** de `ir-spanish/analysis/figures/` (distribución de relevantes por consulta; longitud en palabras de consultas, de documentos relevantes y del corpus completo; en train y test). `capitulos/capitulo4.tex` §4.1 — corregidas tres marcas obsoletas: el «pendiente» del bullet de nivel 3 (línea 7), el `[PENDIENTE: calcular, cf. ir-spanish/analysis/]` de la cabecera `% P3` (que además pedía «percentiles», y el cuaderno calcula desviación estándar y frecuencias) y el `[Opcional: figura de barras…]` del último bullet de P3, que ahora describe las figuras que ya existen. `notes/indice.md` §4.1 — la línea espejo del bullet de nivel 3, actualizada en el mismo sentido para que el índice autoritativo y el capítulo no diverjan. `AGENTS.md` — nueva subsección «Fuente de verdad por repositorio» en «Git» y un bullet en «Ejecución de notebooks».
- **Decisiones:** los bullets de nivel 3 **no** llevan cifras (siguen siendo espejo del índice; el detalle numérico vive en el nivel 4) y la corrección se aplicó en los dos archivos a la vez. Las figuras se copian al repo de la tesis, que ya versiona `tesis.pdf`; **queda pendiente decidir si alguna se incluye en el capítulo**, porque todavía ningún `.tex` las referencia (`\includegraphics` no aparece aún). Se adopta que **cada repositorio tiene una sola máquina escritora**: `ir-spanish` en el servidor (los agentes entran por `ssh iimas` a editar, ejecutar, commitear y publicar), `tesis` y el paraguas en el Mac; el usuario queda fuera de la regla y resuelve a mano si edita los dos lados.
- **Estado / pendientes:** `ir-spanish` publicó `4b0e291` (cuaderno reescrito + 7 figuras + `notes/cmds.sh`) y `cb17f9b` (changelog de ese repo, escrito en el servidor); el clon del Mac quedó como espejo limpio en `cb17f9b`, tras descartar copias verificadas como idénticas (respaldo en `/tmp/espejo-mac-backup/`). En `tesis/` siguen **sin commitear** `capitulos/capitulo4.tex`, `notes/changelog.md`, `figuras/` y `tesis.pdf` (decisión del usuario), y no se recompiló: los cambios de §4.1 son comentarios y no alteran el PDF. Sigue abierta la verificación del total de consultas de la v1.2. **Hallazgo sin resolver:** `knowledge/google-featured-snippets.md:48` mantiene un `**[Pendiente]**` sobre si `expanded_search` añade relevantes por consulta, mientras que la entrada anterior de este changelog da ese cabo por resuelto: el cuaderno mide la distribución en test (donde `expanded_search = False` siempre) y la multiplicidad viene del subconjunto «full» (2.17 % con 2 relevantes, hasta 4), no de `expanded_search`. Falta decidir si eso cierra el pendiente del archivo de `knowledge/` o si hay que comparar con train.

---

## 2026-09-14 — Cap. 4, §4.1: bullets de contenido por párrafo con datos de MessIRve v1.2
- **Cambios:** `tesis/capitulos/capitulo4.tex` §4.1 — bajo cada `% P1`–`% P4` se añadieron bullets (`%   - …`) con las ideas, cifras y citas que debe contener cada párrafo, para que el usuario los convierta en redacción. Cifras del split de prueba de la v1.2 (subconjunto «full») tomadas de `ir-spanish/analysis/distributions.ipynb`: 174 078 juicios, 170 055 consultas únicas, 69 001 documentos relevantes únicos, 51 190 artículos relevantes; corpus eswiki_20240401: 14 047 759 párrafos de 1 946 205 artículos; relevantes por consulta: media 1.02, mediana 1, 97.73 % con exactamente 1; longitudes de consultas (media 5.70 palabras) y documentos. Datos de procedencia y partición verificados contra el artículo (`recursos/messirve-ValentiniEtAl2025.pdf`) y la tarjeta de Hugging Face.
- **Decisiones:** los juicios del split de prueba se declaran binarios en la práctica (en test siempre match_score = 1 y expanded_search = False; en train match_score es continuo en [0.5, 1]). El párrafo P3 ya justifica la batería de métricas con la distribución real (Recall@100 ∈ {0, 1} por consulta; P@50 ≤ 0.02 en el 97.7 % de las consultas; MAP ≈ MRR) — esto resuelve el cabo suelto (1) anotado el 2026-09-13.
- **Estado / pendientes:** verificaciones marcadas como `[VERIFICAR]` en el .tex: (a) la tarjeta de HF atribuye la actualización de julio de 2025 (~200 000 consultas nuevas) a la **v1.1**, no a la v1.2 (que solo añade la columna de títulos) — ajustar la redacción si importa la precisión; (b) el total global de consultas de la v1.2 (train + test) no está calculado (el artículo reporta «casi 700 000» para la v1.0); (c) confirmar si el split de entrenamiento se usó para algo. Entrada de GooAQ (Khashabi et al., 2021) pendiente en `biblio.bib` si se decide citarla. Figuras del notebook exportables a PDF/PNG si se quieren incluir.

---

## 2026-09-13 — Nueva carpeta `knowledge/` para resúmenes de referencia
- **Cambios:** creada `tesis/knowledge/` con `google-featured-snippets.md`: cómo determina Google los *featured snippets* (mecanismo documentado en la patente US11409748B1, dónde entra el comportamiento humano, qué no es público y qué implica para las etiquetas de MessIRve). `AGENTS.md` — `knowledge/` documentado en «Estructura de `tesis/`».
- **Decisiones:** `knowledge/` alberga explicaciones de conceptos y mecanismos **externos** al proyecto, un archivo por tema, cada uno con su sección de fuentes y marcando qué está documentado, qué es reportado y qué quedó sin verificar. Se separa de `notes/`, que son notas de trabajo del proyecto.
- **Estado / pendientes:** el resumen deja dos cabos sueltos. (1) Verificar en el conjunto de datos la distribución de documentos relevantes por consulta: si es 1, Recall@100 solo toma valores en {0, 1}, P@50 queda acotada por 0.02 y MAP se comporta como MRR, lo que afecta a la justificación de la batería de métricas de §4.4 y al párrafo P3 de §4.1. (2) Confirmar en la documentación de Google que las valoraciones de sus evaluadores humanos de calidad no afectan directamente al posicionamiento.

## 2026-09-13 — Migración a biblatex + biber con APA 7 en español
- **Cambios:** `tesis/tesis.tex` — fuera `natbib` y `\bibliographystyle{abbrv}`; entra `\usepackage[backend=biber,style=apa]{biblatex}`, `\addbibresource{biblio.bib}`, `\usepackage{csquotes}` y `\printbibliography[heading=bibintoc]`. `tesis/capitulos/capitulo4.tex:10` — `(\parencite{...})` → `\parencite{...}`, porque los paréntesis manuales duplicaban la cita. `AGENTS.md` — reescrita «Compilación» (obligación del punto 4).
- **Decisiones:** se migró para tener el aparato de citas en **español** y **APA 7** real, que natbib + BibTeX no alcanzaba sin `apacite` (APA 6 y en inglés) ni parchear el `.bst` a mano. `style=apa` (paquete `biblatex-apa`, instalado con `sudo tlmgr install biblatex-apa`) en lugar de `style=authoryear`, que no pone coma antes del año ni invierte todos los apellidos. `csquotes` es obligatorio para ese estilo y ya estaba instalado. Además, el `biber` de TeX Live está **roto** en esta máquina (envoltorio autoextraíble que falla al llamar a `/usr/bin/lipo`): se sustituyó por la slice arm64 extraída en `~/bin/biber` (v2.21), que gana en el `PATH` y que TeXstudio usa porque invoca `biber %` sin ruta absoluta.
- **Estado / pendientes:** verificado en **copia aislada** (27 páginas, 0 errores, 0 citas sin resolver, `spanish-apa.lbx` cargado): cita en el texto `(Valentini et al., 2025)` y referencia `Valentini, F., Cotik, V., … & Pérez, J. M. (2025). …`. **Falta recompilar in situ**: la verificación se hizo en `/tmp` porque TeXstudio estaba corriendo y truncó `tesis.aux` al compilar en paralelo (LaTeX aborta con `! Extra }, or forgotten \endgroup`). En el cuerpo, `\citep`/`\citet` ya **no existen**: usar `\parencite`/`\textcite`. Un `.bbl` generado por BibTeX hace fallar a biblatex (`not created by biblatex`): limpiar con `latexmk -C tesis.tex`.

## 2026-09-13 — Claves de cita normalizadas en `biblio.bib`
- **Cambios:** `tesis/biblio.bib` — las 18 claves renombradas a la convención `<modelo-o-tema>-<AutoresEnPascalCase><Año>`, idéntica al nombre base del PDF en `recursos/`. Solo cambiaron las líneas de apertura `@tipo{clave,`; ningún campo se tocó. Verificado con bibtex (18 entradas, 0 errores) y `latexmk -g -pdf` (exit 0). `AGENTS.md` — punto 3 del protocolo alineado con el criterio nuevo, añadido el punto 4 (una decisión que contradiga el archivo obliga a actualizarlo en la misma sesión, verificando antes cuál de los dos tiene razón), y «Advertencias conocidas» reescritas: las dos que avisaban de entradas heredadas de PLN y de claves inconsistentes quedaron falsas al vaciar el archivo. `tesis/notes/changelog.md` — añadido el criterio de qué merece entrada.
- **Decisiones:** 2 autores → ambos apellidos (`KhattabZaharia`, `FoxShaw`); 3 o más → primer apellido + `EtAl`. El prefijo de tema conserva la capitalización del PDF (`bm25-` en minúsculas; `mE5-`, `spladeV3-` tal cual) y va sin tildes (`isr-MouraoEtAl2014`). La clave es el nombre del PDF: clave ↔ archivo ↔ fuente son el mismo identificador. **La sección «Compilación» de `AGENTS.md` no se tocó:** se pidió cambiarla a biber, pero `tesis/` usa natbib + BibTeX (`tesis.tex:53` y `:213-214`; `tesis.blg` = «This is BibTeX»; sin `.bcf`); el subproyecto con biblatex + biber es `protocolo/`.
- **Estado / pendientes:** `biblio.bib` ya tiene las fuentes principales de `fuentes.md`. Las secundarias se agregarán solo si hacen falta. El cuerpo aún no tiene ningún `\cite`, así que la bibliografía se imprime vacía: es esperado.

## 2026-09-12 — Cap. 4: plan de párrafos (nivel 4) en capitulo4.tex
- **Cambios:** `tesis/capitulos/capitulo4.tex` — añadido el nivel 4 (plan párrafo-por-párrafo) como comentarios `% P<n>:` bajo cada (sub)sección, conservando las viñetas de nivel 3 espejo del índice. `AGENTS.md` — documentada la convención en «Dónde vive el plan». Esta entrada en el changelog.
- **Decisiones:** el nivel 4 vive en cada archivo `.tex` para no contaminar `notes/indice.md`; una línea `% P<n>:` por párrafo que indica qué debe cubrir y qué valores/citas/tablas/figuras insertar, con marcadores `[PENDIENTE: …]` para lo que falta. Desglose: §4.1 (4 párrafos), §4.2 (5), §4.3 (3), §4.4 protocolo común (4), E1/E2/E4 (1 c/u), E3 (2). Valores anclados a `ir-spanish/`: top-100 por modelo en primera etapa, semilla 42, RRF k=60 (default de ranx), ~100 candidatos al reranking, hardware 2× RTX A5000 de 24 GB.
- **Estado / pendientes:** el usuario redacta los párrafos. Pendientes señalados en las notas: distribución de relevantes por consulta (§4.1), Fig. 4.1 del pipeline (§4.2), normalización exacta usada antes de CombMNZ (ver `ir-spanish/rerankers/fuse.py`), medida de eficiencia del E3, entradas de Urbano et al. (2019) y Smucker et al. (2007) en `biblio.bib`, y precisar si E3 incluye jina-colbert-v2.

---

## 2026-09-12 — Índice: protocolo de significancia estadística definido
- **Cambios:** `tesis/notes/indice.md` — §2.5.2 (marco) con cinco viñetas sobre pruebas de significancia; §4.4 desglosa el protocolo de evaluación (métricas, prueba, justificación, comparaciones múltiples); §4.4.4 y §5.4 marcan el E4 como comparación descriptiva sin prueba; §5.2 referencia Tukey HSD. Comentarios de `capitulos/capitulo2.tex`, `capitulo4.tex` y `capitulo5.tex` re-sincronizados con el índice.
- **Decisiones:** prueba principal = **t pareada (Student) de dos colas, α = 0.05** para las hipótesis de efectividad media; **test de permutación** como alternativa robusta (ambas disponibles en `ranx.compare`). Justificación: Urbano et al. (2019) como fuente principal, Smucker et al. (2007) como antecedente. Comparaciones múltiples con **Tukey HSD** (E2). E4 queda como **comparación descriptiva** (los sistemas propietarios no publican puntuaciones por consulta).
- **Estado / pendientes:** falta implementar las pruebas en `ir-spanish/` (hoy solo se usan métricas de `ranx`; la API es `ranx.compare`) y crear las entradas de `biblio.bib` para UrbanoEtAl2019 y SmuckerEtAl2007.

## 2026-09-12 — Fuente de significancia estadística: Urbano et al. (2019)
- **Cambios:** renombrado `recursos/3331184.3331259.pdf` → `recursos/significancia-UrbanoEtAl2019.pdf`; entrada añadida en `tesis/notes/fuentes.md` (**en negrita**: fuente principal del tema); Smucker et al. (2007) queda como fuente de apoyo.
- **Decisiones:** Urbano, Lima y Hanjalic (2019, SIGIR '19) tiene conclusiones más fuertes que Smucker et al. (2007): recomienda **t pareada para hipótesis de efectividad media** (permutación como alternativa), desaconseja **bootstrap-shift** (sesgo hacia p-valores pequeños) y a Wilcoxon/signo para diferencias de medias. Por eso es la fuente principal del aparato de significancia.
- **Estado / pendientes:** falta crear las entradas de `biblio.bib` para UrbanoEtAl2019 y SmuckerEtAl2007 (no existen hoy). Los cambios al índice derivados de esta decisión están en la entrada más reciente (arriba).

## 2026-09-12 — Paso 0: esqueletos sincronizados con el índice
- **Cambios:** reescritos `tesis/capitulos/capitulo1.tex`…`capitulo5.tex` y `conclusiones.tex` con la estructura exacta de `tesis/notes/indice.md` (viñetas de nivel 3 como comentarios `%`); eliminados los epígrafes placeholder y las secciones «Resumen» (no están en el índice); etiquetas uniformes (`sec:…`/`subsec:…`, renombradas las viejas sin referencias); cap. 4 titulado «Metodología»; comentario de `tesis.tex` actualizado.
- **Decisiones:** orden de escritura recomendado y acordado: 0) sincronizar esqueletos (hecho) → 1) cap. 4 Metodología → 2) cap. 5 Resultados (en paralelo con los experimentos pendientes) → 3) cap. 2 Marco teórico → 4) cap. 3 Estado del arte → 5) cap. 1 Introducción → 6) cap. 6 Conclusiones. Método *top-down*: convertir las viñetas en plan párrafo-por-párrafo antes de redactar.
- **Estado / pendientes:** compilación verificada (`latexmk -pdf`, exit 0, 27 páginas). Pendientes para el cap. 4: distribución de relevantes por consulta de MessIRve (§4.1) y nota del límite de 0.6B (§4.2). Discrepancia menor: `indice.md` dice que el cap. 2 tiene 14 subsecciones pero lista 13. Precaución: no compilar en paralelo con el compilador automático del editor (borra los `.aux` a mitad de corrida).
